import { styled } from '@stitches-config'

export const CardRoot = styled('div', {
  borderRadius: '$medium',
  backgroundColor: '$mauve1',
  padding: '$8',
  // border: '$borderWidths$thin solid $colors$mauve3',
  boxShadow: '$soft',
})
